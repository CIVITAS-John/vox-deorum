/**
 * Event triggered during the game's victory testing cycle to allow custom victory condition checks
 * This event takes no parameters as it requires scripts to query game state directly
 */
export interface GameCoreTestVictoryEvent {
  // No parameters - empty event
}